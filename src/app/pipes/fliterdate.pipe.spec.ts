import { FliterdatePipe } from './fliterdate.pipe';

describe('FliterdatePipe', () => {
  it('create an instance', () => {
    const pipe = new FliterdatePipe();
    expect(pipe).toBeTruthy();
  });
});
