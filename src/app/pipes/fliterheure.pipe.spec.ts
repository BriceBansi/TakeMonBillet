import { FliterheurePipe } from './fliterheure.pipe';

describe('FliterheurePipe', () => {
  it('create an instance', () => {
    const pipe = new FliterheurePipe();
    expect(pipe).toBeTruthy();
  });
});
