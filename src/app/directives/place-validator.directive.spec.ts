import { PlaceValidatorDirective } from './place-validator.directive';

describe('PlaceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
